extend layout

block title
  | #{ person.name }

block content
  .page-header
    h1 #{ person.name }

  p #{ person.summary }

  pre
    #{ JSON.stringify( person ) }

  a(href="#{person.slug_url}/edit") Edit
