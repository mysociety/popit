/*  Configuration file
 *
 *
 *
 */

module.exports = {
    MongoDB: {
        prefix: 'popittest_',
    },
    
    // set this so that we can check that the testing config has been loaded
    testing_config_loaded: true,
};
